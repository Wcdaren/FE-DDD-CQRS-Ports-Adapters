import { modulesOrderService } from './modules-order-service';

describe('modulesOrderService', () => {
  it('should work', () => {
    expect(modulesOrderService()).toEqual('modules-order-service');
  });
});
